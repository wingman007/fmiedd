CREATE DATABASE Baza

USE Baza


CREATE TABLE Tablic
(

	ID INT PRIMARY KEY,
	USERNAME VARCHAR(50) NOT NULL,
	PASSWORD VARCHAR(30) NOT NULL,
	EMAIL VARCHAR(30) NOT NULL

	)


		ALTER TABLE Tablic
	ADD Role_ID INT


	CREATE TABLE ROLE
	(

		ID INT FOREIGN KEY REFERENCES Tablic NOT NULL,
		Role VARCHAR(10) NOT NULL

		)

	

